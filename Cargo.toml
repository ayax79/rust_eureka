[package]
name = "eureka"
version = "0.1.0"
authors = ["Jack Wright <jack.wright@nike.com>"]

[dependencies]
hyper = "0.10.9"
url = "1.4.0"
log = "0.3.5"
option-filter = "1.0.1"

serde = "1.0"
serde_derive = "1.0"

# serde_json is just for the example, not required in general
serde_json = "1.0"

